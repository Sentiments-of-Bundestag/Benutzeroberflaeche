// JSON Placeholder
export interface UserInterface {
  id: number;
  name: string;
}

// React Router DOM
export interface NavigationLinkInterface {
  name: string;
  route: string;
}

// Sentiment
export interface Fraction {
  name: string;
  size: number;
}
